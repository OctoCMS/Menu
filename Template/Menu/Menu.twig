{% if menu %}
<ul id="{{ key }}" class="{{ key }}">
    {% for item in menu %}
    <li class="{{ item.current ? "active" : "" }}">
        <a href="{{ item.url }}">{{ item.title }}</a>
    </li>
    {% endfor %}
</ul>
{% endif %}